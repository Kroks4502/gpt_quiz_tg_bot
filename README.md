# Квиз бот

## Commands

Start project:

```sh
docker compose up -d
```

Create migration file:

```sh
scripts/autogenerate_migrations.sh "auto"
```

Run migration:

```sh
scripts/run_migrations.sh
```
